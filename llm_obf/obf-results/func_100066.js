function c(a) {
    var b = a.length - 1, c = 0;
    for (;;) {
        if (RERROR[d()] in U[a]) return c;
        if (a === 0 || b < 2) return !1;
        b -= 2;
        a = a[b];
        c++;
    }
}

// Variable renaming:
const stack = state,
      table = U,
      TERROR = RERROR,
      depth = c,
      stack_probe = b;